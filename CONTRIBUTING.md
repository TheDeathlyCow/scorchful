# Heads up translators! 

The base language files for Scorchful (US English) are generated into `src/main/generated/assets/scorchful/lang`, and are separate from the other language files which should be placed in `src/main/resources/assets/scorchful/lang`.